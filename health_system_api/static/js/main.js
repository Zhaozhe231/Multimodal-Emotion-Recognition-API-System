document.addEventListener('DOMContentLoaded', function() {
    // Initialize any components that need JavaScript
    console.log('Health Emotion Analysis System initialized');

    // File input preview (if needed)
    const faceInput = document.getElementById('face');
    if (faceInput) {
        faceInput.addEventListener('change', function(e) {
            // Logic for previewing face image could go here
        });
    }

    // Add any additional interactive features here
});